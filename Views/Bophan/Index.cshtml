@model IEnumerable<qlnv.Models.Bophan>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenbp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenbp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Mabp">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Mabp">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Mabp">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
