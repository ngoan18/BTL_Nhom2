@model IEnumerable<qlnv.Models.KyLuat>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenKL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenKL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaKL">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaKL">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaKL">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
